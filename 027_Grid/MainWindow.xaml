<Window x:Class="_027_Grid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_027_Grid"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <!-- 제목을 설정하고 윈도우의 크기를 450X400으로 설정한다-->

    <Grid Background="Bisque">
        <!-- 그리드의 바탕을 비스크 색으로 지정한다-->
        <Grid.RowDefinitions>
            <!-- 그리드의 열을 설정하여 열(세로) 를 세개 생성한다 -->
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>-->
        <!--위는 그리드의 행을 설정한 것이다 연습을 위해 주석처리했다 -->

        <!-- 버튼 세개를 열의 순서를 정하여 배치하고-->
        <!-- 좌 상 우 하 순서대로 버튼의 바깥여백을 Margin을 통해 설정한다-->
        <Button Grid.Row="0" Margin="10 10 10 5">Click me! 1</Button>
        <Button Grid.Row="1" Margin="10 5 10 5">Click me! 2</Button>
        <Button Grid.Row="2" Margin="10 5 10 10">Click me! 3</Button>
    </Grid>
</Window>
